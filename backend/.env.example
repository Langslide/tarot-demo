# Required for AI readings (and semantic RAG embeddings).
OPENAI_API_KEY=sk-...

# Optional model overrides (defaults shown).
# TAROT_MODEL=gpt-4o
# TAROT_CLASSIFIER_MODEL=gpt-4o-mini
# TAROT_EMBED_MODEL=text-embedding-3-small
# TAROT_LLM_TIMEOUT=45

# Comma-separated origins for CORS when API and UI run on different hosts.
# TAROT_CORS_ORIGINS=*
